<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View" />

        <variable
            name="isLoading"
            type="boolean" />

        <variable
            name="statusText"
            type="String" />

        <variable
            name="bestHourText"
            type="String" />

        <variable
            name="errorText"
            type="String" />
    </data>

    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorBackground">

        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:fillViewport="true"
            android:paddingHorizontal="@dimen/screen_horizontal"
            android:paddingTop="@dimen/section_spacing"
            android:paddingBottom="@dimen/list_bottom_padding">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <com.google.android.material.card.MaterialCardView
                    android:id="@+id/tonightCard"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/card_spacing_vertical"
                    app:cardBackgroundColor="@color/colorSurface"
                    app:cardCornerRadius="@dimen/card_corner_radius"
                    app:cardElevation="0dp"
                    app:cardPreventCornerOverlap="true"
                    app:strokeColor="@color/outline"
                    app:strokeWidth="@dimen/card_stroke_width">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:padding="@dimen/card_padding">

                        <TextView
                            style="@style/Text.TitleMedium"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:text="@string/tonight_glance_title"
                            android:textColor="?attr/colorOnSurface" />

                        <TextView
                            android:id="@+id/statusLine"
                            style="@style/Text.Body"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="4dp"
                            android:text='@{statusText}'
                            android:textColor="?attr/colorOnSurfaceVariant"
                            android:visibility='@{statusText != null &amp;&amp; !isLoading ? View.VISIBLE : View.GONE}'
                            tools:text="Clear 67% · Moon 0%" />

                        <TextView
                            android:id="@+id/loadingText"
                            style="@style/Text.LabelMedium.Inter"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="2dp"
                            android:text="@string/tonight_refreshing"
                            android:textColor="?attr/colorOnSurface"
                            android:visibility='@{isLoading ? View.VISIBLE : View.GONE}'
                            tools:visibility="gone" />

                        <androidx.recyclerview.widget.RecyclerView
                            android:id="@+id/tonightObjects"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="16dp"
                            android:clipToPadding="false"
                            android:nestedScrollingEnabled="false"
                            android:overScrollMode="never"
                            android:paddingBottom="4dp"
                            android:visibility='@{isLoading ? View.GONE : View.VISIBLE}'
                            tools:listitem="@layout/item_tonight_object" />

                        <TextView
                            android:id="@+id/forecastError"
                            style="@style/Text.Body"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="8dp"
                            android:text='@{errorText}'
                            android:textColor="@color/status_poor_text"
                            android:visibility='@{errorText != null ? View.VISIBLE : View.GONE}'
                            tools:text="Couldn’t load tonight’s forecast."
                            tools:visibility="gone" />

                        <com.google.android.material.divider.MaterialDivider
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginStart="56dp"
                            android:layout_marginTop="16dp"
                            android:layout_marginBottom="12dp"
                            app:dividerColor="@color/outline"
                            app:dividerThickness="1dp" />

                        <androidx.constraintlayout.widget.ConstraintLayout
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="8dp">

                            <TextView
                                android:id="@+id/bestHourLabel"
                                style="@style/Text.LabelMedium.Inter"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="@string/tonight_best_hour_label"
                                android:textColor="?attr/colorOnSurface"
                                app:layout_constraintStart_toStartOf="parent"
                                app:layout_constraintTop_toTopOf="parent"
                                app:layout_constraintBottom_toBottomOf="@id/bestHourValue" />

                            <TextView
                                android:id="@+id/bestHourValue"
                                style="@style/Text.LabelMedium.Inter"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text='@{bestHourText}'
                                android:textColor="?attr/colorOnSurface"
                                app:layout_constraintEnd_toEndOf="parent"
                                app:layout_constraintBaseline_toBaselineOf="@id/bestHourLabel"
                                tools:text="10:00 PM – 11:00 PM" />
                        </androidx.constraintlayout.widget.ConstraintLayout>
                    </LinearLayout>
                </com.google.android.material.card.MaterialCardView>
            </LinearLayout>
        </androidx.core.widget.NestedScrollView>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
</layout>